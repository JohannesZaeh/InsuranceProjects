A C-script to calculate reserves for claims which already happened but were not reported yet,

using the Chain-Ladder-Method which is the DAV Standard.

The script reads the input from a .txt file where the run-off triangle is marked by "-1".

Output is also written to a .txt file.